/**
 * This component defines a reusable `AppButton` for consistent button styling and behavior across the app.
 * 
 * Features:
 * - Touchable button with customizable background color (default: "primary")
 * - Rounded corners and padding for a polished look
 * - Bold, uppercase label text styled for platform-specific fonts
 * - Positioned with `left: 150` and `top: 50`, which may affect layout depending on its parent container
 * 
 * Props:
 * - `title`: button label text
 * - `onPress`: function to execute when the button is tapped
 * - `color`: optional color key from the app's color palette
 * 
 * Note:
 * - The absolute positioning (`left` and `top`) may need adjustment or removal for responsive layouts.
 * 
 * Generated by CoPilot AI
 */

import React, { useRef, useEffect } from "react";
import { StyleSheet, Platform, Text, TouchableOpacity } from "react-native";

import colors from "../config/colors";

function AppButton({ title, onPress, color = "primary" }) {
  return (
    <TouchableOpacity
      onPress={onPress}
      style={[styles.button, { backgroundColor: colors[color] }]}
    >
      <Text style={styles.buttonText}>{title}</Text>
    </TouchableOpacity>
  );
}

const styles = StyleSheet.create({
  button: {
    height: 45,
    width: "100%",
    borderRadius: 35,
    padding: 12,
	left: 150,
	top: 50,
  },
  buttonText: {
    fontSize: 16,
    color: colors.white,
    textTransform: "uppercase",
    fontFamily: Platform.OS === "android" ? "Roboto" : "Avenir",
    fontWeight: "bold",
  },
});

export default AppButton;
